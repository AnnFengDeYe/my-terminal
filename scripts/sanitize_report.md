# Sanitization Report

Generated by `scripts/import_existing_configs.sh --sanitize`.

This report lists categories of replacements only. It intentionally does not include original secrets, tokens, keys, passwords, usernames, emails, or private paths.

- configs/git/gitconfig.example: email address placeholder applied
- configs/git/gitconfig.example: git identity/signing/credential fields normalized
- configs/git/gitconfig: email address placeholder applied
- configs/git/gitconfig: git identity/signing/credential fields normalized
- configs/zsh/zshrc: display name default and private workspace alias normalized
